# Ai-face-swapper
diden test it yet need to test it forst but i just uploaded now bc im on my phone anyway 
# FaceSwap MVP (InsightFace + customtkinter)

## Start
```bash
python -m venv venv
venv\Scripts\activate    # Windows
# source venv/bin/activate  # Linux/macOS
pip install -r requirements.txt
python main.py
